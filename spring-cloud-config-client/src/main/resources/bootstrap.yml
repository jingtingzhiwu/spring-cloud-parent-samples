eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
spring:
  application:
    name: language
  cloud:
    config:
      label: master
      profile: DE
      #uri: http://localhost:8888/
      discovery:
         enabled: true
         serviceId: language-server
server:
  port: 9999
 
management:
  endpoints:
    web:
      exposure:
        include: refresh